import './setup';
import {SSREngine} from '../src/ssr-engine';
import {DOM, PLATFORM} from 'aurelia-pal';

describe('ssr-ebgine', () => {
  describe('render', () => {
    it('should return an HTML string', () => {
      return fail('Test not implemented.');
    });
  });


  it('should run tests', (done) => {
    done();
  });
});
